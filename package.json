{
  "name": "my-person-site-monorepo",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --dir frontend dev",
    "dev:api": "pnpm --dir backend start:dev",
    "build:web": "pnpm --dir frontend build",
    "lint:web": "pnpm --dir frontend lint",
    "lint:api": "pnpm --dir backend lint"
  }
}
